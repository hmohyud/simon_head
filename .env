# Public worker URL (safe to commit — it's just an endpoint).
# Dev prefers the local Vite proxy and falls back to this; production uses it directly.
VITE_CHAT_URL=https://simon-chat.hmohyud.workers.dev
